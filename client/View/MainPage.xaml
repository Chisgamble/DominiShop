<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="DominiShop.View.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">


    <NavigationView x:Name="MainNavView"
                    ItemInvoked="MainNavView_ItemInvoked"
                    IsPaneToggleButtonVisible="True"
                    IsBackButtonVisible="Collapsed"
                    IsSettingsVisible="False">

        <NavigationView.PaneHeader>
            <Grid x:Name="PaneHeaderGrid" 
              Padding="12,20,12,20"
              HorizontalAlignment="Stretch">
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <PersonPicture DisplayName="{x:Bind ViewModel.CurrentUserName, Mode=OneWay}" 
                               ProfilePicture="{x:Bind ViewModel.ProfileImage, Mode=OneWay}"
                               Width="40" Height="40"
                               VerticalAlignment="Center"/>

                    <StackPanel VerticalAlignment="Center" 
                            Visibility="{x:Bind MainNavView.IsPaneOpen, Mode=OneWay}">
                        <TextBlock Text="{x:Bind ViewModel.CurrentUserName, Mode=OneWay}" 
                               Style="{StaticResource BodyStrongTextBlockStyle}"
                               MaxLines="1" TextTrimming="CharacterEllipsis"/>
                        <TextBlock Text="{x:Bind ViewModel.CurrentUserEmail, Mode=OneWay}" 
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"
                               MaxLines="1" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </NavigationView.PaneHeader>

        <NavigationView.MenuItems>
            <NavigationViewItem Content="Thống kê" Icon="FourBars" Tag="StatisticsPage" />
            <NavigationViewItem Content="Kho hàng" Icon="AllApps" Tag="InventoryPage" />
        </NavigationView.MenuItems>

        <Frame x:Name="ContentFrame" />

    </NavigationView>


</Page>

